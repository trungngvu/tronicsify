const ConstrainCheck = ({ cart }: any) => {
  console.log(cart);
  return (
    <div className="flex flex-col flex-grow px-4 py-2 mt-4 bg-white border rounded h-fit"></div>
  );
};

export default ConstrainCheck;
